<%= simple_form_for([:admin, company]) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :location %>
  <%= f.input :employees %>
  <%= f.input :website %>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>