<%= simple_form_for([:admin, office]) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :location %>
  <%= f.input :website %>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>